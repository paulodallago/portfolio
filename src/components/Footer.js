export const Footer = () => {
  const styles = {
    footer: {
      margin: "3vh 0",
    },
  };
  return (
    <div style={styles.footer}>
      Desenvolvido por mim mesmo, obrigado por visitar!
    </div>
  );
};
